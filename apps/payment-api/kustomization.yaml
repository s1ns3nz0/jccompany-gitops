apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: payment-api

resources:
  - serviceaccount.yaml
  - deployment.yaml
  - service.yaml

commonLabels:
  managed-by: argocd
  app.kubernetes.io/managed-by: argocd
